<div>
<div class="fea-colle-slide">
  
  
  <div style="display:none">
    <media-gallery id="MediaGallery-{{ block.id }}" role="region" aria-label="{{ 'products.product.media.gallery_viewer' | t }}" data-desktop-layout="stacked">
      <div id="GalleryViewer-{{ block.id }}" class="product__media-list">
        {%- if product.selected_or_first_available_variant.featured_media != null -%}
        {%- assign media = product.selected_or_first_available_variant.featured_media -%}
        <div class="product__media-item" data-media-id="{{ block.id }}-{{ media.id }}">
        </div>
        {%- endif -%}
        {%- liquid
              assign media_to_render = product.featured_media.id
              for variant in product.variants
                assign media_to_render = media_to_render | append: variant.featured_media.id | append: ' '
              endfor
            -%}
        {%- for media in product.media -%}
        {%- if media_to_render contains media.id and media.id != product.selected_or_first_available_variant.featured_media.id -%}
        <div class="product__media-item" data-media-id="{{ block.id }}-{{ media.id }}">
        </div>
        {%- endif -%}
        {%- endfor -%}
      </div>
    </media-gallery>
  </div>
  
  {% comment %}Speed optimization code start{% endcomment %}
   <img src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=="
       data-srcset="{%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
               {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
               {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
               {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
               {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
               {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
               {{ product.featured_media | image_url }} {{ product.featured_media.width }}w"
       data-src="{{ product.featured_media | image_url: width: 533 }}"
       sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
       alt="{{ product.featured_media.alt | escape }}"
       class="motion-reduce lazyload"
       {% unless lazy_load == false %}loading="lazy"{% endunless %}
       width="{{ product.featured_media.width }}"
       height="{{ product.featured_media.height }}"
       >
    {% comment %}Speed optimization code end{% endcomment %}
  
  
  <div class="product">
    
     

  <h2>{{ product.title | escape }}</h2>
  {%- render 'price', product:product, use_variant: true, show_badges: true, price_class: 'price--large' -%}
 <div class="main-banner-reviews-text">
               <div class=" yotpo bottomLine"
    data-appkey="IU654GhrymKotQO3weIEKxubNZIaRR5d7yfbmWaT"
    data-domain="{{shop.permanent_domain | escape }}"
    {% if product.title contains "Non-Binding" %}data-product-id="{{ 4996292444213 }}"{% endif %}
    {% if product.title contains "Ankle" %}data-product-id="{{ 6715023130677 }}"{% endif %}
    {% if product.title contains "Pain" %}data-product-id="{{ 6795760042037 }}"{% endif %}
    {% if product.title contains "Compression" %}data-product-id="{{ 6731626020917 }}"{% endif %}
    data-product-models="{{ product.id }}"
    data-name="{{ product.title | escape }}"
    data-url="{{ shop.url }}{{ product.url }}"
    data-image-url="{{ product.featured_image | product_img_url: 'large' |replace: '?', '%3F' | replace: '&','%26'}}"
    data-description="{{ product.description | escape }}"
    data-bread-crumbs="{% for tag in product.tags %}{{ tag | escape }};{% endfor %}">
            </div>    </div>
    {%- unless product.has_only_default_variant -%}
    <variant-radios class="no-js-hidden" data-section="{{ block.id }}" data-url="{{ product.url }}" {{ block.shopify_attributes }}>
      {%- for option in product.options_with_values -%}
    
      <fieldset class="js product-form__input variant_option_{{ option.name | handleize }}">
        <legend class="form__label">{{ option.name }}</legend>
		
        <div class="variant_options">
          {%- for value in option.values -%}
          <div class="vari_op">
            <input type="radio" id="{{ block.id }}-{{ option.position }}-{{ forloop.index0 }}"
                   name="{{ option.name }}"
                   value="{{ value | escape }}"
                   form="{{ product_form_id }}"
                   {% if option.selected_value == value %}checked{% endif %}
                   >
            <label for="{{ block.id }}-{{ option.position }}-{{ forloop.index0 }}">
              {{ value }}
            </label>
            
          </div>
          
          
          {%- endfor -%}
        </div>
        
        	 	<!-- This is for Size Guide Popup --><!--
 {% if product.title contains "Binding" or product.title contains "Compression" %}                       
  <div class="interior">
     {% if product.title contains "Binding" %}  
    <a class="btn pro-size_guide center" href="#open-modal">{{ section.settings.size_guide }}</a>
    {% endif %}
    {% if product.title contains "Compression" %}  
    <a class="btn pro-size_guide center" href="#open-modal">{{ section.settings.size_guide }}</a>
    {% endif %}
  </div>

<div id="open-modal" class="modal-window">
  <div class="container">
   <a  href="#" class="modal-close" title="Close"><div>&#10006;</div></a>
    <div class="sizeguide-Container row">
      
      
    
      {% if product.title contains "Binding" %}
      <div class="non-binding">
      <div class="col-xs-12">
       
	<img src="https://cdn.shopify.com/s/files/1/0253/3641/4261/files/Sock_Sizing_Chart_V3_-_For_Website_Sock_Sizing_Chart_-_Full.png?v=1654541006">
        <p> All Viasox Diabetic Socks and Compression Socks come in both thin and thick Ply weave, running from sizes <b>Medium</b>, <b>Large</b>, and <b>Extra Large</b>, with our sock lengths starting as short as <b>Ankle</b>, <b>Crew</b>, and <b>Knee High</b>.</p>
      </div>
      <div class="col-xs-7">
        <img src="https://cdn.shopify.com/s/files/1/0253/3641/4261/files/Sock_Sizing_Chart_V3_-_For_Website_Sock_Sizing_Chart_-_Full_copy.png?v=1654541005">
        <img src="https://cdn.shopify.com/s/files/1/0253/3641/4261/files/Sock_Sizing_Chart_V3_-_For_Website_Sock_Sizing_Chart_-_Full_copy_3_59178184-9284-4faf-bb74-4f1e5af7d86c.png?v=1654541570">
      </div>
      <div class="col-xs-5">
        <img src="https://cdn.shopify.com/s/files/1/0253/3641/4261/files/Sock_Sizing_Chart_V3_-_For_Website_Sock_Sizing_Chart_-_Full_copy_5.png?v=1654541005">
      </div>
      </div>
      {% endif %}
      {% if product.title contains "Compression" %}
      <div class="compression">
      <div class="col-xs-12">
        <img src="https://cdn.shopify.com/s/files/1/0253/3641/4261/files/Sock_Sizing_Chart_V3_-_For_Website_Sock_Sizing_Chart_-_Full.png?v=1654541006">
        
      </div>
      <div class="col-xs-4">
        <img src="https://cdn.shopify.com/s/files/1/0253/3641/4261/files/Sock_Sizing_Chart_V3_-_For_Website_Sock_Sizing_Chart_-_Full_copy_2.png?v=1654546767">
      </div>
      
      <div class="col-xs-8">
        <p> All Viasox Diabetic Socks and Compression Socks come in both thin and thick Ply weave, running from sizes <b>Medium</b>, <b>Large</b>, and <b>Extra Large</b>.<br>
         Compression Diabetic socks ONLY come in knee high.</p>
      </div>
    </div>
      {% endif %}
      
      
      
          </div>
        </div>
        </div>   
          {% endif %}-->
   <!-- This is for Size Guide Popup --> 
      
        
        
        <!-- <a class="pro-size_guide center" href="{{ section.settings.size_guide_link }}">{{ section.settings.size_guide }}</a>-->

        
        
        
        
      </fieldset>
      {%- endfor -%}
      <script type="application/json">
                    {{ product.variants | json }}
      </script>
    </variant-radios>
    {%- endunless -%}

    <noscript class="product-form__noscript-wrapper-{{ block.id }}">
      <div class="product-form__input{% if product.has_only_default_variant %} hidden{% endif %}">
        <label class="form__label" for="Variants-{{ block.id }}">{{ 'products.product.product_variants' | t }}</label>
        <div class="select">
          <select name="id" id="Variants-{{ block.id }}" class="select__select" form="{{ product_form_id }}">
            {%- for variant in product.variants -%}
            <option
                    {% if variant == product.selected_or_first_available_variant %}selected="selected"{% endif %}
                    {% if variant.available == false %}disabled{% endif %}
                    value="{{ variant.id }}"
                    >
              {{ variant.title }}
              {%- if variant.available == false %} - {{ 'products.product.sold_out' | t }}{% endif %}
                        - {{ variant.price | money | strip_html }}
            </option>
            {%- endfor -%}
          </select>
          {% render 'icon-caret' %}
        </div>
      </div>
    </noscript>

    <div>
      {%- if product != blank -%}
      <product-form class="product-form">
        <div class="product-form__error-message-wrapper" role="alert" hidden>
          <svg aria-hidden="true" focusable="false" role="presentation" class="icon icon-error" viewBox="0 0 13 13">
            <circle cx="6.5" cy="6.50049" r="5.5" stroke="white" stroke-width="2"/>
            <circle cx="6.5" cy="6.5" r="5.5" fill="#EB001B" stroke="#EB001B" stroke-width="0.7"/>
            <path d="M5.87413 3.52832L5.97439 7.57216H7.02713L7.12739 3.52832H5.87413ZM6.50076 9.66091C6.88091 9.66091 7.18169 9.37267 7.18169 9.00504C7.18169 8.63742 6.88091 8.34917 6.50076 8.34917C6.12061 8.34917 5.81982 8.63742 5.81982 9.00504C5.81982 9.37267 6.12061 9.66091 6.50076 9.66091Z" fill="white"/>
            <path d="M5.87413 3.17832H5.51535L5.52424 3.537L5.6245 7.58083L5.63296 7.92216H5.97439H7.02713H7.36856L7.37702 7.58083L7.47728 3.537L7.48617 3.17832H7.12739H5.87413ZM6.50076 10.0109C7.06121 10.0109 7.5317 9.57872 7.5317 9.00504C7.5317 8.43137 7.06121 7.99918 6.50076 7.99918C5.94031 7.99918 5.46982 8.43137 5.46982 9.00504C5.46982 9.57872 5.94031 10.0109 6.50076 10.0109Z" fill="white" stroke="#EB001B" stroke-width="0.7"/>
          </svg>
          <span class="product-form__error-message"></span>
        </div>

        {%- form 'product', product, id: product_form_id, class: 'form', novalidate: 'novalidate', data-type: 'add-to-cart-form' -%}
        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" disabled>
        <div class="product-form__buttons">
          <button
                  type="submit"
                  name="add"
                  class="product-form__submit button button--full-width {% if block.settings.show_dynamic_checkout and product.selling_plan_groups == empty %}button--secondary{% else %}button--primary{% endif %}"
                  {% if product.selected_or_first_available_variant.available == false %}disabled{% endif %}
                  >
            <span>
              {%- if product.selected_or_first_available_variant.available -%}
              {{ 'products.product.add_to_cart' | t }}
              {%- else -%}
              {{ 'products.product.sold_out' | t }}
              {%- endif -%}
            </span>
            <div class="loading-overlay__spinner hidden">
              <svg aria-hidden="true" focusable="false" role="presentation" class="spinner" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
                <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
              </svg>
            </div>
          </button>
          {%- if block.settings.show_dynamic_checkout -%}
          {{ form | payment_button }}
          {%- endif -%}
        </div>
        {%- endform -%}
      </product-form>
      {%- else -%}
      <div class="product-form">
        <div class="product-form__buttons form">
          <button
                  type="submit"
                  name="add"
                  class="product-form__submit button button--full-width button--primary"
                  disabled
                  >
            {{ 'products.product.sold_out' | t }}
          </button>
        </div>
      </div>
      {%- endif -%}
    </div>
  </div>

</div>


  
<script>
  document.addEventListener('DOMContentLoaded', function() {
    function isIE() {
      const ua = window.navigator.userAgent;
      const msie = ua.indexOf('MSIE ');
      const trident = ua.indexOf('Trident/');

      return (msie > 0 || trident > 0);
    }

    if (!isIE()) return;
    const hiddenInput = document.querySelector('#{{ product_form_id }} input[name="id"]');
    const noScriptInputWrapper = document.createElement('div');
    const variantSwitcher = document.querySelector('variant-radios[data-section="{{ block.id }}"]') || document.querySelector('variant-selects[data-section="{{ block.id }}"]');
    noScriptInputWrapper.innerHTML = document.querySelector('.product-form__noscript-wrapper-{{ block.id }}').textContent;
    variantSwitcher.outerHTML = noScriptInputWrapper.outerHTML;

    document.querySelector('#Variants-{{ block.id }}').addEventListener('change', function(event) {
      hiddenInput.value = event.currentTarget.value;
    });
  });
</script>
  <style>
    /* Size Guid CSS */

.modal-window {
  position: fixed;
  background-color: rgba(0, 0, 0, 0.5);
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 999;
  visibility: hidden;
  opacity: 0;
  pointer-events: none;
  transition: all 0.3s;
}
.modal-window:target {
  visibility: visible;
  opacity: 1;
  pointer-events: auto;
}
.modal-window > div {
      width: 600px;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    padding: 0rem 4rem;
    background: white;
}
.modal-window header {
  font-weight: bold;
}
.modal-window h1 {
  font-size: 150%;
  margin: 0 0 15px;
}
.modal-close {
    color: #000;
    /* line-height: 60px; */
    font-size: 80%;
    position: absolute;
    right: 0;
    text-align: center;
    top: 0;
    /* font-size: 50px; */
    /* width: 60px; */
    /* text-decoration: none; */
    transform: scale(3);
    padding: 20px;
}
.modal-close:hover {
  color: #FF8548;
 
}

.modal-window > div {
  border-radius: 1rem;
}
.modal-window div:not(:last-of-type) {
  margin-bottom: 0px;
}
.logo {
  max-width: 150px;
  display: block;
}
.btn {
  background-color: white;
  padding: 1em 1.5em;
  border-radius: 0.5rem;
  text-decoration: none;
}
.btn i {
  padding-right: 0.3em;
}
   
  .modal-window a:focus,.modal-window a:hover {
    
     text-decoration: none;
    
  }

.non-binding p{
 text-align:center;
  font-size: 22px;
    font-weight: 500;
  padding-left:2rem;
  padding-right:2rem;
  padding-top:0.5rem;
  padding-bottom:0.5rem;
}
.compression p {
    text-align: center;
    font-size: 22px;
    font-weight: 500;
    padding: 2rem 2rem;
  
}
/* Size Guid CSS End */
  </style>
</div>


